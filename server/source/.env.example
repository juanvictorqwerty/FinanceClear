# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=your_db_name

# Server Configuration
PORT=5000

# Google Sheets API Configuration
# Choose ONE of the following methods:

# Method 1: Service Account Key File (recommended for development)
# Download the JSON key file from Google Cloud Console and provide the path
GOOGLE_SERVICE_ACCOUNT_KEY_FILE=path/to/your/service-account-key.json

# Method 2: Service Account Credentials as JSON String (recommended for production)
# Copy the entire content of your service account JSON file as a string
# GOOGLE_SERVICE_ACCOUNT_CREDENTIALS={"type":"service_account","project_id":"your-project",...}

# Optional: Default spreadsheet ID to use
# GOOGLE_SHEETS_DEFAULT_SPREADSHEET_ID=your_default_spreadsheet_id

# JWT Secret (move this from hardcoded value)
JWT_SECRET=jWTrandomstringSecretUserToken1sttimeIuseitlearNINGEveryDay147

FRONTEND_URL=your_frontend_url_for_production
# Admin Secret Key
ADMIN_SECRET_KEY=your_secret_key

# Email Configuration
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_email_password